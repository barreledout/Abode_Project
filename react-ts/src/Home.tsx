import React from "react";
import HeroImg from "./Hero";
import ServiceCards from "./ServiceCards";

const Home = () => {
  return (
    <section>
      <HeroImg />
      <ServiceCards />
    </section>
  );
};

export default Home;
